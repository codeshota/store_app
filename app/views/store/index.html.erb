<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h1>Catalog</h1>

<% @products.each do |product| %>
  <div class="entry">
    <%= image_tag(product.image_url) %>
    <h3><%= product.title %></h3>
    <p><%= product.description %></p>
    <p><%= product.price %></p>
  </div>
<% end %>